export interface IIcon {
  width: number;
  height: number;
  stroke?: string;
}

export interface ILocationInfo {
  imag: string;
  title: string;
  price: number;
  option?: string[];
  comment: number;
  ownerImg: string;
  rate: number;
}
